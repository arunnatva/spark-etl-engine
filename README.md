this code repo has the modules that convert informatica mapping XMLs into JSON configuration that can be understood by a spark ETL engine program. and the ETL engine will read the
configuration, and execute the DAG 
